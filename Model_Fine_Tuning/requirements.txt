# Fine-Tuning Dependencies for AEGIS-SQL
# Model fine-tuning on BIRD benchmark for Text-to-SQL

# Core Training Framework
torch>=2.0.0
transformers>=4.36.0
accelerate>=0.25.0
peft>=0.7.0                    # Parameter-Efficient Fine-Tuning (LoRA)
trl>=0.7.0                     # Transformer Reinforcement Learning
datasets>=2.14.0               # HuggingFace datasets

# Quantization & Optimization
bitsandbytes>=0.41.0           # 8-bit/4-bit quantization
einops>=0.7.0                  # Tensor operations
sentencepiece>=0.1.99          # Tokenization for some models

# Training Utilities
wandb>=0.15.0                  # Experiment tracking (optional)
tensorboard>=2.14.0            # Training visualization
scipy>=1.10.0                  # Scientific computing

# Data Processing
pandas>=2.0.0
numpy>=1.24.0
huggingface-hub>=0.19.0        # HF Hub upload/download

# Evaluation
sqlglot>=20.0.0                # SQL parsing
func-timeout>=4.3.5            # Execution timeout

# Existing AEGIS dependencies (if not already installed)
pydantic>=2.0.0
pyyaml>=6.0.0
loguru>=0.7.0

# Optional: Flash Attention 2 for faster training (requires compatible GPU)
# flash-attn>=2.3.0
