# AEGIS-SQL Dependencies
# Three-Axis Constrained Optimization for Hybrid NL2SQL

# Core Framework
langgraph>=0.2.0
langchain>=0.3.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
pyyaml>=6.0.0
python-dotenv>=1.0.0

# Logging
loguru>=0.7.0

# Embeddings & Retrieval (Schema Extraction)
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4  # OR faiss-gpu for GPU support
FlagEmbedding>=1.2.0  # For BGE-M3 hybrid retrieval
numpy>=1.24.0
torch>=2.0.0  # For embeddings

# Local SLM (FSLM)
transformers>=4.35.0
accelerate>=0.25.0
peft>=0.7.0  # For LoRA adapters
bitsandbytes>=0.41.0  # For quantization

# Remote LLM (FLLM)
openai>=1.0.0
anthropic>=0.8.0

# SQL Parsing & Verification
sqlglot>=20.0.0
sqlparse>=0.4.0

# Database
# sqlite3 is built-in to Python (no need to install)

# Evaluation
func-timeout>=4.3.5
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0

# Development
black>=23.0.0
isort>=5.12.0
flake8>=6.1.0
mypy>=1.7.0

# Optional: For GPU acceleration
# torch>=2.0.0+cu118  # CUDA 11.8
# faiss-gpu>=1.7.4
